<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spiderman.calnories">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashscreen.SpalshActivity"
            android:label="@string/app_name">

            <!-- android:theme="@style/AppTheme.NoActionBar" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signInGoogle.LoginActivity" />
        <activity android:name=".calories.CaloriesActivity" />
        <activity android:name=".food.FoodActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".detailfood.DetailFoodActivity" />
        <activity android:name=".sports.SportsActivity"></activity>
    </application>

</manifest>